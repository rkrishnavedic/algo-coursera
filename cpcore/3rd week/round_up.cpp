#include <bits/stdc++.h>
#include <vector>

#define fori(n) for(long long i=0;i<n;i++)
#define ll long long

using namespace std;

int main(){
        //cout<<"Om\n";
        long long n,k,g;
        cin>>n>>k;
        g=ceil((double)n/k);
        cout<<g;
        
        return 0;
    }
